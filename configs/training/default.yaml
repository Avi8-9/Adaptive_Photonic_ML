optimizer:
  name: Adam
  learning_rate: 0.001
  beta1: 0.9
  beta2: 0.999

batch_size: 64
epochs: 100
validation_fraction: 0.10
early_stopping_patience: 20

loss:
  classification: cross_entropy
  physical_regularization: phase_gradient
  lambda: 0.001

reproducibility:
  seed: 42
